# Prevent esbuild postinstall from downloading/validating a native binary in environments
# where executing freshly-written binaries can fail with ETXTBSY ("text file busy").
# This avoids post-processing/install failures in container/shared-filesystem contexts.
ESBUILD_SKIP_BINARY_DOWNLOAD=1
